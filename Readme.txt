1.  To install the Semantic Kernel package, you can use the following command in your terminal:
    dotnet add package Microsoft.SemanticKernel

1.1 Or you may install it via the NuGet Package Manager in Visual Studio by searching for "Microsoft.SemanticKernel".

2. After installing the package, you can run this code to verify that the Semantic Kernel is set up correctly.
